// Define a function to print each magnician name from an Array
function show_magnicians(magnicians: string[]){
    magnicians.forEach(name => console.log(name));
}

//Define an array containing magnicians name
let magnician_names = ["Harry Porter", "Mifra", "Wania"];

//Call the function to print each magnician names 
show_magnicians(magnician_names);